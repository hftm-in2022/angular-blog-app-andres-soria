import { Routes } from '@angular/router';
import { DemoComponent } from './demo.component';

const DEMO_PAGE_ROUTES: Routes = [
  {
    path: '',
    component: DemoComponent,
  },
];

export default DEMO_PAGE_ROUTES;
